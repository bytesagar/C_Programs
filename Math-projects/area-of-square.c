#include <stdio.h>
int main()
{
    int length, area;
    printf("Enter The length: ");
    scanf("%d",&length);
    area = length * length;
    printf("The area of square is %d.",area);
    return 0;
}
